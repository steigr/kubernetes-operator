{
  "name": "website",
    "scripts": {
        "build": "echo $BASE_URL; hugo --minify --baseURL=${BASE_URL}"
    },
    "devDependencies": {
    "autoprefixer": "^10.4.18",
    "postcss": "^8.4.35",
    "postcss-cli": "^11.0.0"
  }
}
